"use strict";
describe('Data Type', () => {
    it("should must declare", () => {
        let name = "Robby Maulana";
        let balance = 100000000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
