describe('Data Type', ()=> {
    it("should must declare", ()=> {
        let name: string = "Robby Maulana";
        let balance: number = 100_000_000;
        let isVip: boolean = true;

        console.info(name)
        console.info(balance)
        console.info(isVip)
    })
})